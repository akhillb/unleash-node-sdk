name: Build and test

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20.x, 22.x, 24.x]

    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Setup node
        uses: actions/setup-node@v6
        with:
          node-version: ${{matrix.node-version}}
          cache: yarn
      - name: Yarn
        run: yarn
      - name: Test
        run: yarn lint && yarn test

  report-coverage:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Setup node
        uses: actions/setup-node@v6
        with:
          node-version: 24.x
          cache: yarn
      - name: Yarn
        run: yarn
      - name: Test
        run: yarn coverage
      - name: Coveralls
        uses: coverallsapp/github-action@v2
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          fail-on-error: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  test-as-dependency:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Setup node
        uses: actions/setup-node@v6
        with:
          node-version: 24.x
          cache: yarn
      - name: Yarn
        run: yarn
      - name: Pack SDK tarball
        run: |
          SDK_TARBALL="$(npm pack --silent --ignore-scripts | tail -n 1)"
          echo "SDK_TARBALL=$SDK_TARBALL" >> "$GITHUB_ENV"
      - name: Setup node for CJS compatibility check
        uses: actions/setup-node@v6
        with:
          # Pin to a pre-require(esm) Node 20 release to catch ESM-only transitive deps
          # that would otherwise pass on newer Node 20 patches.
          node-version: 24.19.0
      - name: Test install in other package
        run: ./scripts/test-package/run.sh "file:../${SDK_TARBALL}"
